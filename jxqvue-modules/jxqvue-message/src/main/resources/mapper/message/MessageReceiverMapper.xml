<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxqvue.message.mapper.MessageReceiverMapper">
    
    <resultMap type="MessageReceiver" id="MessageReceiverResult">
        <result property="id"    column="id"    />
        <result property="messageId"    column="message_id"    />
        <result property="receiverId"    column="receiver_id"    />
        <result property="receiverName"    column="receiver_name"    />
        <result property="readStatus"    column="read_status"    />
        <result property="readTime"    column="read_time"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectMessageReceiverVo">
        select id, message_id, receiver_id, receiver_name, read_status, read_time, create_by, create_time, update_by, update_time, remark from message_receiver
    </sql>

    <select id="selectMessageReceiverList" parameterType="MessageReceiver" resultMap="MessageReceiverResult">
        <include refid="selectMessageReceiverVo"/>
        <where>  
            <if test="messageId != null "> and message_id = #{messageId}</if>
            <if test="receiverId != null "> and receiver_id = #{receiverId}</if>
            <if test="receiverName != null  and receiverName != ''"> and receiver_name like concat('%', #{receiverName}, '%')</if>
            <if test="readStatus != null  and readStatus != ''"> and read_status = #{readStatus}</if>
        </where>
        order by create_time desc
    </select>
    
    <select id="selectMessageReceiverById" parameterType="Long" resultMap="MessageReceiverResult">
        <include refid="selectMessageReceiverVo"/>
        where id = #{id}
    </select>

    <select id="selectMessageReceiverByMessageId" parameterType="Long" resultMap="MessageReceiverResult">
        <include refid="selectMessageReceiverVo"/>
        where message_id = #{messageId}
    </select>
        
    <insert id="insertMessageReceiver" parameterType="MessageReceiver" useGeneratedKeys="true" keyProperty="id">
        insert into message_receiver
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="messageId != null">message_id,</if>
            <if test="receiverId != null">receiver_id,</if>
            <if test="receiverName != null and receiverName != ''">receiver_name,</if>
            <if test="readStatus != null">read_status,</if>
            <if test="readTime != null">read_time,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null and updateBy != ''">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="messageId != null">#{messageId},</if>
            <if test="receiverId != null">#{receiverId},</if>
            <if test="receiverName != null and receiverName != ''">#{receiverName},</if>
            <if test="readStatus != null">#{readStatus},</if>
            <if test="readTime != null">#{readTime},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null and updateBy != ''">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateMessageReceiver" parameterType="MessageReceiver">
        update message_receiver
        <trim prefix="SET" suffixOverrides=",">
            <if test="messageId != null">message_id = #{messageId},</if>
            <if test="receiverId != null">receiver_id = #{receiverId},</if>
            <if test="receiverName != null and receiverName != ''">receiver_name = #{receiverName},</if>
            <if test="readStatus != null">read_status = #{readStatus},</if>
            <if test="readTime != null">read_time = #{readTime},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteMessageReceiverById" parameterType="Long">
        delete from message_receiver where id = #{id}
    </delete>

    <delete id="deleteMessageReceiverByMessageId" parameterType="Long">
        delete from message_receiver where message_id = #{messageId}
    </delete>

    <delete id="deleteMessageReceiverByIds" parameterType="String">
        delete from message_receiver where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 统计相关SQL -->
    <select id="countUnreadMessages" resultType="java.lang.Long">
        select count(*) from message_receiver where read_status = '0'   
    </select>

    <select id="countUserMessages" parameterType="Long" resultType="java.lang.Long">
        select count(*) from message_receiver where receiver_id = #{userId}
    </select>

    <select id="countUserUnreadMessages" parameterType="Long" resultType="java.lang.Long">
        select count(*) from message_receiver where receiver_id = #{userId} and read_status = '0'
    </select>

    <select id="countUserReadMessages" parameterType="Long" resultType="java.lang.Long">
        select count(*) from message_receiver where receiver_id = #{userId} and read_status = '1'
    </select>

    <update id="markAsReadByUserIdAndMessageId">
        update message_receiver 
        set read_status = '1', read_time = now(), update_time = now()
        where receiver_id = #{param1} and message_id = #{param2}
    </update>

    <update id="markAsUnreadByUserIdAndMessageId">
        update message_receiver 
        set read_status = '0', read_time = null, update_time = now()
        where receiver_id = #{param1} and message_id = #{param2}
    </update>

    <delete id="deleteByUserIdAndMessageId">
        delete from message_receiver 
        where receiver_id = #{param1} and message_id = #{param2}
    </delete>
</mapper>
